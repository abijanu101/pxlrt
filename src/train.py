'''
    if preprocessed data not found in resources:
        call preprocessing subroutine
    
    if model found, raise warning
    if confirmed to train, train
    persist into models/gan.pk1 or whatever
'''